# Define the minimum acceptable security level for ptrace_scope
# 0 = Classic permissions (Least secure)
# 1 = Restricted to children (Standard secure baseline)
# 2 = Admin-only attach (CAP_SYS_PTRACE required)
# 3 = No attach allowed (Highest security)
MIN_SECURE_LEVEL=1
